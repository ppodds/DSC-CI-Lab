{
  "scripts": {
    "test": "jest",
    "lint": "eslint src",
    "lint-fix": "eslint --fix src",
    "format": "prettier --write src",
    "prepare": "husky install"
  },
  "lint-staged": {
    "src/**/*.js": [
      "prettier --write",
      "eslint --fix"
    ]
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "husky": "^7.0.4",
    "jest": "^27.5.1",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.0"
  }
}
